repos:
  - repo: local
    hooks:
      - id: pytest
        exclude: ^tests/
        name: pytest
        entry: pytest
        language: system
        types: [python]
        pass_filenames: false
        always_run: true


